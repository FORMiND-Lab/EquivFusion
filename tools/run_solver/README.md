# exec_solver

## Usage
```
exec_solver --solver <solver_name> --inputfile <inputfile> [--opts <opts>]
```

## Options
- `--solver <solver_name>` specifies a solver_name
- `--inputfile <inputfile>` specifies an input file
- `--opts <opts>` specifies options for the solver